CREATE TABLE FAMILY (ID INTEGER NOT NULL, DESCRIPTION VARCHAR(255), PRIMARY KEY (ID))
CREATE TABLE PERSON (ID VARCHAR(255) NOT NULL, LASTNAME VARCHAR(255), NONSENSEFIELD VARCHAR(255), FIRSTNAME VARCHAR(255), FAMILY_ID INTEGER, PRIMARY KEY (ID))
CREATE TABLE PERSON_JOB (Person_ID VARCHAR(255) NOT NULL, jobList_ID INTEGER NOT NULL, PRIMARY KEY (Person_ID, jobList_ID))
CREATE TABLE JOB (ID INTEGER NOT NULL, SALERY FLOAT, JOBDESCR VARCHAR(255), PRIMARY KEY (ID))
ALTER TABLE PERSON ADD CONSTRAINT PERSON_FAMILY_ID FOREIGN KEY (FAMILY_ID) REFERENCES FAMILY (ID)
ALTER TABLE PERSON_JOB ADD CONSTRAINT PERSONJOBjobListID FOREIGN KEY (jobList_ID) REFERENCES JOB (ID)
ALTER TABLE PERSON_JOB ADD CONSTRAINT PERSONJOBPerson_ID FOREIGN KEY (Person_ID) REFERENCES PERSON (ID)
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT DECIMAL(15), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN_TABLE', 0)
